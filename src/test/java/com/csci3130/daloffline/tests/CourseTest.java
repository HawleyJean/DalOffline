package com.csci3130.daloffline.tests;

import org.junit.Test;

public class CourseTest {

	@Test
	public void courseTest() {
		//TODO these are not actually in the code, but they should be
		//long crn = -1;
		//addCourse(crn);
		//assertEquals(true, isInCourseTable(crn));
		//removeCourse(crn);
		//assertNotEquals(true, isInCourseTable(crn));
	}
}
